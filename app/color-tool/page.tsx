import { ColorTool } from '@/components/ColorTool';

export default function ColorToolPage() {
  return <ColorTool />;
}

export const metadata = {
  title: 'Color Wheel & Design Tool',
  description: 'Professional color wheel tool with combinations, contrast checking, and palette export functionality.',
};
